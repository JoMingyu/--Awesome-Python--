# 내장 함수도 중요하지만 어떤 외부 모듈이 있는지 아는 것도 중요하다
# 이 때 이거 쓰면 돼 정도만 알면 된다
# External module에선 os 모듈을 살펴보자
import os
print(os.environ)
print(os.getcwd())

# 필요한 모듈이 있다면 구글링을 하자(필요한 기능은 대부분 다 있다)
# 친절한 스택오버플로우 답변들이 보일 거다
